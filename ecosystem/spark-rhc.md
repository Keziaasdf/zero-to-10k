# ecosystem/spark-rhc.md — Spark en Robinhood Chain

**Estado:** VIGILAR + CONTENIDO. No es airdrop confirmado.
**Última actualización:** 2026-09-06

## Qué es
- **Spark Protocol `$SPK`** (Base/Ethereum, "Spark Protocol" DeFi de MakerDAO/Sky): airdrop **CERRADO**, claim vencía **2025-12-17**. → **Cero hunt de claim SPK.** No confundir con nada de RHC.
- **Spark en RHC** (`@usespark_` / usespark.fun): launchpad / hook de **Uniswap v4** sobre Robinhood Chain. Distinto del `$SPK` de arriba. Se trackea como **experimento de ecosistema + tema de contenido**, no como drop prometido.
- **Seedify `vibe/vibe` Season 0** (RHC **testnet 46630**): launchpad donde se crean/tradean tokens de prueba. Seedify prometió **5% del NUEVO `$SFUND`** a testnet users + token creators, ETA ~sept 2026. Es el único componente con reward "confirmado" (por @meta_alchemist, no contrato).
  - NFTs "Vibe Vibers": 200k `$SPARK` = 1 NFT (la wallet del usuario tiene ~400k → 2). Reglas revisadas varias veces (stakers tier 6+, Phoenix Sale). Meta dijo que publicaría una tool para calcular NFTs+SFUND — correr la wallet ahí **cuando salga**.

## Insumo local (solo lectura)
- `../spark-testnet/` — playbook testnet: `interactions-checklist.md`, `next-token-creates.md`, drafts X, `research/` (leaderboard tactics, posts de Meta sobre SFUND).
- `../robinhood-chain/RESEARCH.md` §0b — la aclaración de los 3 tokens Seedify (old SFUND muerto, SPARK = tech token, nuevo SFUND = launchpad token).
- Wallet única: `0xE427…F9Ae`. **NO sybil.**

## Tesis (para 0→10k)
1. **Contenido:** el meta "3 tokens Seedify que todos confunden" + "cómo farmear la Season 0 gratis" es un explicador con demanda. Sirve de dealflow para `@KeziaQl1`.
2. **Experimento watch:** si el nuevo $SFUND o Spark-en-RHC anuncia snapshot/contrato real con fecha, se evalúa como spike (no como apuesta) — siempre camino gratis (testnet + gas mínimo).

## Próxima acción
- Draft de post/hilo "los 3 tokens Seedify" y "farmeo gratis Season 0" → `content/drafts/`.
- `experiments/002-spark-rhc-watch/` mantiene la lista de contratos/docs/X oficiales + señales de un drop FUTURO + scams a ignorar.

## Kill
- Si el nuevo $SFUND lanza y el 5% a testnet users **no se materializa** o exige capital → cerrar el frente, quedarse solo con el contenido ya publicado.
- Si Seedify pide cualquier depósito/conexión de wallet a un sitio nuevo → parar.

## No hacer
- No conectar la wallet a ningún sitio "de claim".
- No crear segunda wallet para farmear (los retrodrops de Seedify filtran sybil → quema la principal).
- No comprar $SPARK / $SFUND / NFTs con capital real sin `PROPOSAL.md` + OK.
- No tratar "ETA septiembre" como fecha firme en el PIPELINE.
